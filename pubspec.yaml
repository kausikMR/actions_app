name: actions_app
description: Actions App for Github Actions testing and for fun.
publish_to: 'none'
version: 0.1.0+6

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
